---
import Layout from '../layouts/Layout.astro'
import { TrendingUp, Lightbulb, MessageSquare } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { TeamMember, OrganizationalChart, CultureCompany } from '@/components/about/nosotros'
import { Header } from '@/components/header'
import Footer from '@/components/footer.astro'

import { teamMembers } from '@/constants/teamMembers'
---

<Layout>
	<div class="min-h-screen bg-white">
		<Header client:load />

		<section class="bg-gradient-to-br from-primary via-[#FB6300] to-primary text-white py-20">
			<div class="container mx-auto px-4 text-center">
				<h1 class="text-5xl md:text-6xl font-black mb-6">Conoce a Nuestro Equipo</h1>
				<p class="text-base font-bold italic mb-8 max-w-3xl mx-auto">
					Somos 3 desarrolladores apasionados por la tecnología, unidos por la visión de llevar la tecnología a la mano de
					nuestros clientes.
				</p>
				<div class="flex justify-center space-x-4">
					<Button className="bg-white text-primary hover:bg-card px-8 py-3 font-bold cursor-pointer">Ver Proyectos</Button>
					<a href="/contacto"
						><Button className="bg-white text-primary hover:bg-card px-8 py-3 font-bold cursor-pointer"> Contáctanos</Button></a
					>
				</div>
			</div>
		</section>

		{/* Team Members */}
		<section class="py-16 bg-card">
			<div class="container mx-auto px-4">
				<div class="text-center mb-12">
					<h2 class="text-4xl font-bold mb-4">Miembros</h2>
					<p class="text-gray-600 max-w-2xl mx-auto">
						Conocenos a los profesionales que hacen posible CODARI. Cada miembro aporta su experiencia única y pasión por la
						innovación.
					</p>
				</div>

				<div class="grid lg:grid-cols-3 gap-8">
					{teamMembers.map((member) => <TeamMember member={member} />)}
				</div>
			</div>
		</section>

		{/* Organizational Chart */}
		<section class="py-16 bg-background">
			<div class="container mx-auto px-4">
				<OrganizationalChart />
			</div>
		</section>

		{/* Company Culture */}
		<CultureCompany />

		{/* Footer */}
		<Footer />
	</div>
</Layout>
