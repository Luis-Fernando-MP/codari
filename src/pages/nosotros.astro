---
import Layout from '../layouts/Layout.astro'
import { TrendingUp, Lightbulb, MessageSquare } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { TeamMember, OrganizationalChart } from '@/components/about/nosotros'
import { Header } from '@/components/header'
import Footer from '@/components/footer.astro'

import { teamMembers } from '@/constants/teamMembers'
---

<Layout>
	<div class="min-h-screen bg-white">
		<Header client:load />

		<section class="bg-gradient-to-br from-primary via-[#FB6300] to-primary text-white py-20">
			<div class="container mx-auto px-4 text-center">
				<h1 class="text-5xl md:text-6xl font-black mb-6">Conoce a Nuestro Equipo</h1>
				<p class="text-base font-bold italic mb-8 max-w-3xl mx-auto">
					Somos 3 desarrolladores apasionados por la tecnología, unidos por la visión de llevar la tecnología a la mano de
					nuestros clientes.
				</p>
				<div class="flex justify-center space-x-4">
					<Button className="bg-white text-primary hover:bg-card px-8 py-3 font-bold cursor-pointer">Ver Proyectos</Button>
					<a href="/contacto"
						><Button className="bg-white text-primary hover:bg-card px-8 py-3 font-bold cursor-pointer"> Contáctanos</Button></a
					>
				</div>
			</div>
		</section>

		{/* Team Members */}
		<section class="py-16 bg-card">
			<div class="container mx-auto px-4">
				<div class="text-center mb-12">
					<h2 class="text-4xl font-bold mb-4">Miembros</h2>
					<p class="text-gray-600 max-w-2xl mx-auto">
						Conocenos a los profesionales que hacen posible CODARI. Cada miembro aporta su experiencia única y pasión por la
						innovación.
					</p>
				</div>

				<div class="grid lg:grid-cols-3 gap-8">
					{teamMembers.map((member) => <TeamMember member={member} />)}
				</div>
			</div>
		</section>

		{/* Organizational Chart */}
		<section class="py-16 bg-background">
			<div class="container mx-auto px-4">
				<OrganizationalChart />
			</div>
		</section>

		{/* Company Culture */}
		<section class="py-16">
			<div class="container mx-auto px-4">
				<div class="max-w-4xl mx-auto text-center">
					<h2 class="text-3xl font-bold mb-8">Nuestra Cultura de Trabajo</h2>
					<p class="text-gray-600 text-lg mb-12">
						En CODARI creemos en la colaboración, la innovación constante y el crecimiento personal y profesional de nuestro
						equipo.
					</p>

					<div class="grid md:grid-cols-3 gap-8">
						<Card className="group hover:shadow-lg transition-all duration-300">
							<CardContent className="p-6 text-center">
								<div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
									<Lightbulb className="w-8 h-8 text-blue-600" />
								</div>
								<h3 class="text-xl font-bold mb-3">Innovación</h3>
								<p class="text-gray-600">Fomentamos la creatividad y la experimentación con nuevas tecnologías y metodologías.</p>
							</CardContent>
						</Card>

						<Card className="group hover:shadow-lg transition-all duration-300">
							<CardContent className="p-6 text-center">
								<div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
									<MessageSquare className="w-8 h-8 text-green-600" />
								</div>
								<h3 class="text-xl font-bold mb-3">Colaboración</h3>
								<p class="text-gray-600">Trabajamos en equipo, compartiendo conocimientos y apoyándonos mutuamente.</p>
							</CardContent>
						</Card>

						<Card className="group hover:shadow-lg transition-all duration-300">
							<CardContent className="p-6 text-center">
								<div class="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
									<TrendingUp className="w-8 h-8 text-purple-600" />
								</div>
								<h3 class="text-xl font-bold mb-3">Crecimiento</h3>
								<p class="text-gray-600">Promovemos el desarrollo continuo y el aprendizaje de nuevas habilidades.</p>
							</CardContent>
						</Card>
					</div>
				</div>
			</div>
		</section>

		{/* Footer */}
		<Footer />
	</div>
</Layout>
